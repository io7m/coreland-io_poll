20060727 version: io_poll 0.1
20060912 code: moved to latest coreproject
20060912 code: added installer
20060921 code: fixed incorrect conversion to nanoseconds in kqueue
               wait(). imported int64.h for times as most values
               are too large for kqueue on 32 bit hardware.
20061010 code: if len == 0, io_poll_add now correctly inserts at
               start of array as opposed to skipping.
20061026 code: OVERALLOCATE() macro to stop epoll_create(0) ever
               happening.
20061026 code: wait() instantly returns 0 if iop->len == 0.
20061026 code: more tests
20061026 version: io_poll 0.101
